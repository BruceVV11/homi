# Homi 0.10.0+14 — Commercial guardrails and emergency-region foundation

Date: 2026-09-11
Status: Windows Flutter gate passed; governed 0.10 backend deployment passed; S25 Ultra device acceptance pending

## Why this pass exists

0.9.2 established the approved People/Safety experience and Google Places integration. 0.10.0 creates the technical boundaries required before Homi can safely add paid continuous-location plans or expand emergency shortcuts beyond South Africa.

This pass deliberately does **not** pretend Google Play billing is active. No client-only purchase callback grants entitlement.

## Homi+ commercial contract

The source records the approved first pricing/seat model:

- Free: R0, no paid continuous sender seat once enforcement is activated;
- Homi+ Personal: R19.99/month, one sender seat;
- Homi+ Duo: R34.99/month, two sender seats under one payer;
- Homi+ Household: R49.99/month or R499.99/year, up to four shared-Household members plus future/full Household cloud synchronization;
- every paid sender seat has up to five active trusted live viewers;
- receiving another person's shared location remains free;
- Duo members do not need to live in the same home;
- privacy, revoke, stop-sharing, check-in disable and deletion are never paywalled.

The plan model intentionally contains no Google Play product IDs until the Play catalog exists.

## Continuous-location cost protection

- client latest-location cloud-write guard raised from 30 seconds to 90 seconds;
- Firestore independently rejects latest-location repeat writes inside 90 seconds;
- `setLocationShare` keeps its deployed callable name but has a hard maximum of five active outbound live-location viewers per sender;
- disabling/revoking a viewer remains available even after a relationship is gone and is not blocked by the activation rate limiter;
- existing 30 trusted connections remain possible — only simultaneously active outgoing Live viewers are capped;
- latest-state/no-route-history architecture is unchanged.

Paid entitlement enforcement is intentionally deferred until Google Play server verification exists, so this beta correction does not strand current testers behind a fake local paywall.

## Emergency regions

- emergency numbers are modeled as an offline, source-controlled catalog;
- leading-zero numbers are represented as strings;
- no global South Africa fallback exists;
- device locale can suggest a supported region without requesting location permission;
- onboarding adds an Emergency region confirmation step;
- the setting persists locally and drives both Safety and full-map emergency controls;
- countries without one safe universal number use service-specific numbers rather than an invented SOS target;
- Home/Work Google Places autocomplete uses the selected region rather than the previous hardcoded South Africa restriction.

Public distribution in each country remains gated on release-candidate review against ITU-T E.129 and/or the relevant national authority.

## Existing 0.9.2 contracts preserved

- People remains map-first;
- exact Home/Work visibility remains separately opt-in;
- arrival notifications remain address/coordinate-free;
- check-in-only background sampling still does not refresh cloud latest location unless Live updates are independently enabled;
- emergency actions still use external phone-app handoff only;
- Google Places setup remains private-key driven and separate from Maps rendering;
- local-first household data remains intact.

## Windows Flutter validation checkpoint

Bruce's Windows Flutter toolchain fully passed the 0.10 client gate on exact app/runtime source `11df198bfc5801257cef45dc4bba64e9d37772d7`:

- `flutter pub get` succeeded;
- `flutter analyze` -> **No issues found**;
- `flutter test` -> **41/41 passed**;
- no tracked local source drift was reported.

Later Cloud Shell deployment-tool corrections modified only governed release tooling/documentation and did not modify Flutter app source, Functions runtime source, Firestore rules, or the already-validated dependency graph. The Windows gate therefore carries forward and should not be repeated merely because deployment tooling moved the repository head.

## Governed backend deployment

The accepted backend deployment ran from exact GitHub source/tooling SHA:

`fba7479a8304aec30a964f32cfd8b3e268b6c78e`

Proven during the refresh-safe Cloud Shell worker:

- Node `v22.23.2`;
- Firebase/GCP project `homi-ee80a`, project number `883068189841`;
- Functions dependency install/verification completed;
- exactly 24 Homi Function exports were validated before deployment;
- Firestore security emulator suite passed **13/13**, including the new 90-second location-update boundary;
- Firestore rules compiled and were released successfully;
- Firestore indexes deployed successfully;
- all 24 Node 22 Gen2 Homi Functions deployed in five controlled batches;
- `setLocationShare(africa-south1)` updated successfully;
- final `setLocationShare` state verified **ACTIVE**;
- worker status ended **PASS**.

The repeated `GOOGLE_CLOUD_QUOTA_PROJECT is not usable when uploading source for Cloud Functions` line was non-fatal; each Functions source upload and deployment batch completed successfully.

Two earlier worker attempts failed before deployment due release-tooling compatibility/preflight ordering. They did not change production. The third worker is the accepted deployment evidence.

## Remaining 0.10 acceptance proof

1. S25 Ultra launch/regression of the normal Homi shell, People, Maps, Places and check-ins.
2. Verify Emergency region selection/settings and representative dial targets without completing emergency calls.
3. Verify the full-map emergency controls update after changing region.
4. Verify five active live-location viewers succeed and a sixth is rejected with product wording; deactivation always works.
5. Verify existing live location, hearts and exact Home/Work privacy behavior remains healthy.

Do not mark 0.10.0 fully device-accepted until those device gates are proven.
