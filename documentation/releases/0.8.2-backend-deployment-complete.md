# Homi 0.8.2+10 — backend deployment completion

Date: 10 September 2026

Bruce confirmed that the corrected batched Cloud Shell deployment completed successfully after the 0.8.2 security-closure deployment repairs.

## Proven deployment path

The successful path retained all of the 0.8.2 release gates and fixes:

- Node 22 release guard;
- temporary Functions dependency/cache workspace;
- generated synchronized Functions lockfile;
- local `npm ci` preflight;
- Function source syntax validation;
- Firestore Emulator security gate at 12/12 passing;
- Firestore rules/index deployment;
- completed migration from stale HTTPS `onConnectionDeleted` to the Firestore-triggered `onTrustedConnectionDeleted` replacement;
- Functions deployment in batches of five to avoid the provider request burst that affected the earlier broad deployment.

## Result

The 0.8.2 Functions backend is no longer considered deployment-pending. The next source pass may build on the protected collaboration boundary, dedicated runtime identity and batched deployment helper.

This does not by itself prove the 0.8.2 Flutter client on Bruce's physical Android toolchain. Device/analyzer/test gates remain separate and must only be marked complete from actual toolchain evidence.
