# Homi 0.9.0+11 — backend deployment complete

Date: 2026-09-10

## Candidate

The application/backend candidate validated for this deployment was:

`c7e7b86656bc650ce1c8f0aabbb5a3129319db3c`

## Flutter validation

Bruce confirmed the post-cleanup Windows gate passed on the candidate above:

- `flutter analyze` — clean;
- `flutter test` — all tests passed.

The earlier dependency resolution had also completed successfully.

## Backend deployment

Bruce then ran the governed Homi backend helper from Cloud Shell:

```bash
cd ~/homi
git pull
bash scripts/deploy-notification-backend.sh
```

Bruce reported that the deployment completed and he did not observe any failures during the run.

This is recorded as **user-confirmed successful deployment**. The full Cloud Shell log was not supplied in chat, so this note does not claim independent log-by-log verification of every provider response.

The deployment candidate includes the new App-Check-protected `sendArrivalCheckIn` callable in `africa-south1`, along with the already-proven batched Function deployment path and Firestore security gate.

## Current release state

Backend publication is no longer the 0.9 blocker. The next gate is physical-device acceptance on the Samsung S25 Ultra, followed by a second Android device for background-location/check-in behaviour.

Still requiring device proof:

- emergency call handoff for 112 / 10111 / 10177;
- Household vs Friends & trusted people grouping;
- profile photos/fallbacks in People and Task assignment;
- fresh-install notification permission/default state;
- Home/Work configuration and recipient selection;
- real outside → inside arrival notification delivery;
- no false arrival on restart/open while already inside a saved zone;
- interaction between Live updates and Arrival check-ins;
- People-notification opt-out;
- disconnected-recipient handling;
- existing hearts, connections, shared Tasks, live location, developer notifications and account lifecycle regression.

Do not call 0.9.0 accepted until the directly affected device workflows pass.